# Common steps

 - Run Frontend app located in viewer/examples
 - Run signaling server, located in broadcaster
 - Click receive

## Camera
 - Run script `webrtc_camera.py`

## SHM
 - Run script in parent directory `camera.py` 
 - Run `webrtc_shm.py`